export const INITIAL_ATTENDANCE_MODAL = { open: false, handleAction: null };

export const INITIAL_DISCARD_MODAL = { open: false, handleAction: null };

export const INITIAL_GEO_LOCATION_ACTION = {
  open: false,
  handleAction: null,
  customer_id: null,
};

export const INITIAL_CHECKOUT_MODAL = {
  open: false,
  handleAction: null,
  handleCheckOut: null,
  openConfirm: true,
};
